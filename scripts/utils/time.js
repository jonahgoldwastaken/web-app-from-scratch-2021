export { convertMStoS }

/**
 * Converts milliseconds to seconds
 * @param {number} milliseconds
 * @returns {number} Converted milliseconds into seconds
 */
function convertMStoS(milliseconds) {
  return milliseconds / 1000
}
